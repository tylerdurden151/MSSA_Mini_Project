// Single place for the backend's base URL, so every fetch call in the app
// points at the same server. Update this if the API's port ever changes.
export const API_BASE_URL = "https://localhost:7118";
